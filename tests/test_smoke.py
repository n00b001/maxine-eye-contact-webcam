"""Placeholder test while the repo is being rewritten around
the unified in-container GPU pipeline. Real tests land once the
new Docker-based pipeline is in place.
"""


def test_placeholder() -> None:
    assert True
