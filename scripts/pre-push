#!/usr/bin/env bash
set -euo pipefail

echo "Running pre-push checks..."

# Coverage gate is off during the unified-pipeline rewrite — runtime
# code has moved into the Docker image.
uv run pytest

echo "Pre-push checks passed."
